package louis.greendaodemo.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import louis.greendaodemo.greendao.entity.SyncManager;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SYNC_MANAGER".
*/
public class SyncManagerDao extends AbstractDao<SyncManager, Void> {

    public static final String TABLENAME = "SYNC_MANAGER";

    /**
     * Properties of entity SyncManager.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Key = new Property(0, String.class, "Key", false, "KEY");
        public final static Property Value = new Property(1, String.class, "value", false, "VALUE");
    }


    public SyncManagerDao(DaoConfig config) {
        super(config);
    }
    
    public SyncManagerDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SYNC_MANAGER\" (" + //
                "\"KEY\" TEXT," + // 0: Key
                "\"VALUE\" TEXT);"); // 1: value
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SYNC_MANAGER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SyncManager entity) {
        stmt.clearBindings();
 
        String Key = entity.getKey();
        if (Key != null) {
            stmt.bindString(1, Key);
        }
 
        String value = entity.getValue();
        if (value != null) {
            stmt.bindString(2, value);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SyncManager entity) {
        stmt.clearBindings();
 
        String Key = entity.getKey();
        if (Key != null) {
            stmt.bindString(1, Key);
        }
 
        String value = entity.getValue();
        if (value != null) {
            stmt.bindString(2, value);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public SyncManager readEntity(Cursor cursor, int offset) {
        SyncManager entity = new SyncManager( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // Key
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1) // value
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SyncManager entity, int offset) {
        entity.setKey(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setValue(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(SyncManager entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(SyncManager entity) {
        return null;
    }

    @Override
    public boolean hasKey(SyncManager entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
